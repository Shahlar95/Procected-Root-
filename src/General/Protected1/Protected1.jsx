// import React from 'react';
// import { Navigate, Outlet } from "react-router-dom";



// const Protected1 = () => {
    
//     const localData = JSON.parse(localStorage.getItem('localUser'))
//     console.log(localData)
//     const findData = localData.find(element => {
//         return (element.user === "shahlar" && element.psw === 1);
//     })
    
//  return Boolean(findData)? <Outlet/> : <Navigate to="/login"/>
// }

// export default Protected1